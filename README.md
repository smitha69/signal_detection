## Serverless Setup

### Install dependencies
npm install

### Run locally
serverless offline start

### Available endpoints
GET /health   → Check service status
GET /detect   → Run signal detection
GET /signals  → View saved signals